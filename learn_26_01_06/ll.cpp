#include <iostream>
using namespace std;

/*
    making all the linked list operations:

    1. Creating a linked list.
    2. Traversing throught the linked list.
    3. Insert at (start, end, position).
    4. Read at (start, end, position).
    5. Update at (start, end, position).
    6. Remove at (start, end, position).
    7. Sorting a linked list.
*/

int main() {
    cout << "Hello, world!" << endl;
    return 0;
}
